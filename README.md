# TranslateLite

A simple Raycast extension for translation.

## Usage

1. Clone this repository using `git clone`
2. Use the Raycast `Import Extension` command
3. Select the cloned folder
4. In the Raycast Extensions page, find the extension and enter your API key
